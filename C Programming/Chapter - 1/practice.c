//Write a program to calculate a area of a square
#include "stdio.h"
int main(){
    /*
    float length;
    printf("Enter side\n")
    scanf("%f", &length);
    printf("Area of square of length %f is %f", length, length * length);
     */

    //Write a program to calculate area of a circle
    float radius;
    printf("Enter radius\n");
    scanf("%f", &radius);
    printf("Area of cricle is : %f", 3.14 * radius * radius);



    return 0;
}

