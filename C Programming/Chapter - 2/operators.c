/* LOGIC OPERATORS
 * && AND
 * || OR
 * ! NOT
*/
#include "stdio.h"
int main(){
    int a, b;
    a = 5;
    b = 5;
    a = a + 2;
    b /= 2;
    printf("%d", b);
}


// ASSIGNMENT OPERATORS
/*
 * =
 * +=
 * -=
 * *=
 * /=
 *
 */
//They work the same as python. they simplify the expression of type "a = a + 1"
//into a += 1