// write a program to check if a number is divisible by 2 or not.
#include "stdio.h"
#include "math.h"
int main(){
/*
    // write a program to check if a number is divisible by 2 or not.
    int input;
    scanf("%d", &input);
//  printf("%d", !(input % 2));
    OR printf("%d", input % 2 == 0);
    */


/*
//     2. Write a program to check if number is even or odd
    int input;
    7
    scanf("%d", &input);
    printf("%d", (input % 2) == 0);
    */


    /*
     * 3. is this statement valid
     * char stars == '**'
     // This statement is not valid because 'char' can only hold 1 character
    // more than 1 symbol will take invalid memory space.
     */

//    Q. check if a number isf greater than 9 and less than 0.
    int x;
    scanf("%d", &x);
    printf("%d \n", x>9 && x<100);
}